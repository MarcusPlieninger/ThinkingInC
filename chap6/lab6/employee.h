/* employee.h */

/* addEmployee reads each field from standard
 * input into the next available Employee slot,
 * as in the exercise in the previous section.
 * It returns the index of the Employee just
 * added, or -1 if the array is full. */
int addEmployee(void);

/* print Employee also returns the index of the
 * Employee jus tprinted, or -1 if the index i
 * is invalid */
int print Employee(int i);

/* Does what it says:  */
int numEmployees(void);